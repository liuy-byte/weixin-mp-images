让 Claude Code、Codex CLI 这类工具改代码，发出任务只是第一步。接下来还得确认：改了哪些文件、跑了什么命令，Git Diff 能不能提交。

ccgui 把会话、文件、执行过程和 Git 操作放在同一个桌面工作台里。它适合已经在用这些 CLI、但不想一直翻终端输出的人。

界面让过程更容易看清，但账号和权限仍需自行配置，提交前也要自己检查改动。

项目地址：https://github.com/zhukunpenglinyutong/desktop-cc-gui
